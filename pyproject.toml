[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "design-library"
version = "0.1.0"
description = "Filesystem-backed storage helpers for 3D design assets."
readme = "README.md"
authors = [{ name = "Generated by GPT-5 Codex" }]
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["design_library"]
